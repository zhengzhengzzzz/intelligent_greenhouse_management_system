<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hist.greenHouse.mapper.UserMapper">
    <select id="findUserByName" resultType="User">
        select * from user where username = #{username}
    </select>
    <select id="findUserById" resultType="User">
        select * from user where id = #{id}
    </select>
    <insert id="addUser" parameterType="User">
        insert into user values (#{id},#{username},#{password},#{phone},#{role})
    </insert>
    <delete id="deleteUser">
        delete from user where id = #{id}
    </delete>
    <select id="getUsers" parameterType="Integer" resultType="User">
        select * from user limit #{pageNum},#{pageSize}
    </select>
    <select id="getUsersCount" resultType="Integer">
        select count(*) from user
    </select>
    <update id="updateUser" parameterType="User">
        update user set username = #{username},phone = #{phone},password = #{password},role = #{role}
        where id = #{id}
    </update>
    <insert id="saveUser" parameterType="User">
        insert into user (id,username,password,phone,role) values (#{id},#{username},#{password},#{phone},#{role})
    </insert>
</mapper>